using TestesUnitarios.Desafio.Console.Services;
Console.WriteLine("Hello, World!");
//ValidacoesString validaString = new ValidacoesString();
//string s2 = "Esse é um texto qualquer";
//Console.WriteLine(validaString.ContemCaractere(s2, "teste"));
//string s = "Matrix";
//Console.WriteLine(validaString.RetornarQuantidadeCaracteres(s));

/*
using System;
using System.Collections.Generic;
using TestesUnitarios.Desafio.Console.Services;


            var validacoesLista = new ValidacoesLista();

            List<int> lista = new List<int> { 1, -2, 3, -4, 5 };

            // Teste do método RemoverNumerosNegativos
            var listaSemNegativos = validacoesLista.RemoverNumerosNegativos(lista);
            Console.WriteLine("Lista sem números negativos:");
            listaSemNegativos.ForEach(Console.WriteLine);

            // Teste do método ListaContemDeterminadoNumero
            int numeroParaVerificar = 3;
            bool contemNumero = validacoesLista.ListaContemDeterminadoNumero(lista, numeroParaVerificar);
            Console.WriteLine($"A lista contém o número {numeroParaVerificar}? {contemNumero}");

            // Teste do método MultiplicarNumerosLista
            int multiplicador = 2;
            var listaMultiplicada = validacoesLista.MultiplicarNumerosLista(lista, multiplicador);
            Console.WriteLine("Lista multiplicada:");
            listaMultiplicada.ForEach(Console.WriteLine);

            // Teste do método RetornarMaiorNumeroLista
            int maiorNumero = validacoesLista.RetornarMaiorNumeroLista(lista);
            Console.WriteLine($"Maior número da lista: {maiorNumero}");

            // Teste do método RetornarMenorNumeroLista
            int menorNumero = validacoesLista.RetornarMenorNumeroLista(lista);
            Console.WriteLine($"Menor número da lista: {menorNumero}");

                        var validacoesString = new ValidacoesString();

                        string texto = "Hello, World!";
                        string textoProcurado = "World";
                        string textoProcuradoNoFim = "World!";

                        // Teste do método RetornarQuantidadeCaracteres
                        int quantidadeCaracteres = validacoesString.RetornarQuantidadeCaracteres(texto);
                        Console.WriteLine($"Quantidade de caracteres no texto: {quantidadeCaracteres}");

                        // Teste do método ContemCaractere
                        bool contemCaractere = validacoesString.ContemCaractere(texto, textoProcurado);
                        Console.WriteLine($"O texto contém '{textoProcurado}'? {contemCaractere}");

                        // Teste do método TextoTerminaCom
                        bool terminaComTexto = validacoesString.TextoTerminaCom(texto, textoProcuradoNoFim);
                        Console.WriteLine($"O texto termina com '{textoProcuradoNoFim}'? {terminaComTexto}");
                */